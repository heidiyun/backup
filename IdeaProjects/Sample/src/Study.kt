//lambda
// Function Literal, 함수를 선언하지 않고 곧바로 식으로 전달해서 표현하는 것.
// 사용 이유
// 동작을 변수에 저장하거나 함수의 인자로 넘겨야 할 경우 익명 클래스나 람다를 사용합니다.
//fun sendEmail(person: Person, message: String) {}
//
//val nextAction = ::sendEmail
//val action = { person: Person, message:String -> nextAction}
